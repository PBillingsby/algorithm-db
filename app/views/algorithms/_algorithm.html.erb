<hr className="pt-4"></hr>
<span className="inline-block">
  <label>Select Language</label>
  <%= select_tag "examples", options_for_select(@languages), prompt: '----', onchange: "changeLanguage(this, #{@algorithm.id})", onload: "changeLanguage(this, #{@algorithm.id})" %>
  <%= @language %>
  <div id="exampleSelect" class="mt-3">
    <!-- JS renders examples here -->
  </div>
</span>